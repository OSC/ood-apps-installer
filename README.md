Rake tasks

```
rake build_apps_parallel   # Build all available apps in parallel
rake build_apps_serial     # Build all available apps in serial
rake default               # Build all available applications in parallel
rake deploy                # Copies the application to the deployment path
rake install               # Copies all application assets to the deployment path
rake install_activejobs    # Clone and build activejobs
rake install_dashboard     # Clone and build dashboard
rake install_file-editor   # Clone and build file-editor
rake install_files         # Clone and build files
rake install_myjobs        # Clone and build myjobs
rake install_shell         # Clone and build shell
rake update                # Update all available applications in parallel
rake update_activejobs     # Update and rebuild activejobs
rake update_apps_parallel  # Update all available apps in parallel
rake update_apps_serial    # Update all available apps in serial
rake update_dashboard      # Update and rebuild dashboard
rake update_file-editor    # Update and rebuild file-editor
rake update_files          # Update and rebuild files
rake update_myjobs         # Update and rebuild myjobs
rake update_shell          # Update and rebuild shell
```
